import { avgGrade } from "./lettersscript";
import { gpa } from "./lettersscript";

DOMSelectors.makecard.addEventListener("click", function () {
  let avgGrade = DOMSelectors.avgGrade.value;
  let gpa = DOMSelectors.gpa.value;
});
